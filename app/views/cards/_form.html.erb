<%= simple_form_for @card do |f| %>
  <%= f.input :original_text, label: 'Original text field' %>
  <%= f.input :translated_text, label: 'Translated text field' %>
  <%= f.date_select :review_date, as: :date, order: [:day, :month, :year] %>
  <%= f.button :submit %>
<% end %>